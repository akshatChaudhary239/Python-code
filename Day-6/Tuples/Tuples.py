# Tuples are also used to store mulyiple values in a single variable
# We cannot change elements in a tuple means it is immutable

# the only differences in list and tuple is of brackets and immutabilty

a = (10,20,30,40,50)
print(len(a))